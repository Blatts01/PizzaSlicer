This scripts have to be run inside the Vitis ai docker from xilinx.
The vaild images are next to the pretrained caffemodel and prototxt. 
The prototxt file is changed to laod the images from the roboflow validation dir.

To adapt the network to your DPU Setup, just modify the custom.json file for DPU configuration 
